# Shared security headers.
#
# IMPORTANT: nginx only inherits add_header from an outer level when the inner
# level defines NO add_header of its own. Any location block that sets its own
# add_header (e.g. Cache-Control) therefore silently drops ALL of these unless
# it includes this file too. Include it in every such block.
#
# CSP notes:
#   style-src / font-src must allow Google Fonts — App.jsx and Landing.jsx
#     @import IBM Plex Sans, Space Mono and Syne from fonts.googleapis.com,
#     which serves the font files themselves from fonts.gstatic.com.
#   connect-src allows *.up.railway.app so the browser reaches the backend API
#     directly (VITE_API_URL is baked in at build time). This also covers the
#     self-hosted /api/analytics/event beacon.
add_header X-Frame-Options "DENY" always;
add_header X-Content-Type-Options "nosniff" always;
add_header Referrer-Policy "strict-origin-when-cross-origin" always;
add_header Permissions-Policy "geolocation=(), microphone=(), camera=()" always;
add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
add_header Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; connect-src 'self' https://*.up.railway.app; img-src 'self' data: https://flagcdn.com; font-src 'self' data: https://fonts.gstatic.com; object-src 'none'; base-uri 'self'; frame-ancestors 'none'" always;

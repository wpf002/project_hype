# Exact versions tested in this deploy — bump deliberately after validating.
# To upgrade: update versions here, run `pip install -r requirements.txt` in a
# venv, run the test suite, then commit.
# Future improvement: generate with `pip-compile --generate-hashes` (pip-tools)
# or `uv lock` for fully reproducible, hash-pinned builds.
fastapi==0.136.3
uvicorn==0.48.0
httpx==0.28.1
python-dotenv==1.2.2
asyncpg==0.31.0
anthropic==0.105.2
slowapi==0.1.9
